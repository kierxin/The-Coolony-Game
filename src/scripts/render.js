function board () {

    const gameGrid = document.querySelector(".game-grid");

    // const boardGrid = new Array(30);

    // gameGrid.append("<div>boardGrid</div>");

    // for (let i = 0; i < boardGrid.length; i++) {
    //     const gridCell = boardGrid[i]
    //     gameGrid.appendChild();
    //     gridCell.style.borderColor = "yellow";

    // }

    // console.log("yay");
}