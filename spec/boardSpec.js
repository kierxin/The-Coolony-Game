describe("Board creation", function() {
    it("randomly chooses 1 of the hardcoded starter board arrays", function() {
        // expect(Board.gridRender).toBeFalsy;
    });
    it("assigns the starter board array as the player's startBoard", function() {

    });
    it("randomly distributes 5Gs, 10Os, and 10Cs throughout a 10x14 array", function() {

    });
    it("appends the 10x14 array onto the player's startBoard", function() {
        //each subarray of the 10x14 array should become a subarray of the startBoard; they should all be at the same level of nesting... 
            //in other words, each subArray of the startBoard should not contain further subArrays
    })
})